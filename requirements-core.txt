# Core runtime dependencies for new_pipeline
# Install with: pip install -r requirements-core.txt
#
# Note: Transitive dependencies (numpy sub-dependencies, etc.) are
# automatically resolved and installed by pip. This file contains
# only the packages explicitly imported in the codebase.

darts==0.38.0
keras==3.11.3
lifelines==0.30.0
numpy==2.3.4
pandas==2.3.3
plotly==6.3.0
pybaseball==2.2.7
pytorch_lightning==2.5.2
scikit_learn==1.7.2
scipy==1.16.2
shap==0.48.0
sktime==0.39.0
tensorflow==2.20.0
torch==2.8.0+cu128
torchmetrics==1.8.2
